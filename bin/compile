#!/bin/bash

#if [[ ! -v "TAILSCALE_AUTHKEY" ]]; then
  #echo "error: must set TAILSCALE_AUTHKEY variable in order to move forward"
  #exit 1
#fi

BUILD_DIR=$1
CACHE_DIR=$2
BUILDPACK_DIR="$(dirname "$(dirname "$0")")"

mkdir -p $BUILD_DIR/bin
tar -xzvf "$BUILDPACK_DIR/$STACK.tgz" -C "$BUILD_DIR/bin"
mkdir -p $BUILD_DIR/var/run/tailscale $BUILD_DIR/var/cache/tailscale $BUILD_DIR/var/lib/tailscale
